#include <stdio.h>
#include <stdlib.h>

#include "tokenizer.h"
#include "linkedlist.h"
#include "token.h"

int main() {
    /*
    LinkedList *linkedList = malloc(sizeof(LinkedList));
    initLinkedList(linkedList);

    Token *token0 = malloc(sizeof(Token));
    token0->type = OPEN_TYPE;

    Token *token1 = malloc(sizeof(Token));
    token1->type = INT_TYPE;
    token1->i = 1;

    Token *token2 = malloc(sizeof(Token));
    token2->type = INT_TYPE;
    token2->i = 2;

    Token *token3 = malloc(sizeof(Token));
    token3->type = INT_TYPE;
    token3->i = 3;

    Token *token4 = malloc(sizeof(Token));
    token4->type = CLOSE_TYPE;

    appendToken(linkedList, token0);
    appendToken(linkedList, token1);
    appendToken(linkedList, token2);
    appendToken(linkedList, token3);
    appendToken(linkedList, token4);

    displayList(linkedList);

    cleanupLinkedList(linkedList);
    */

    exit(handleReturn());
}
